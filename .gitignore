__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.eggs/
*.egg
.env
# The template is meant to be committed — the runbook's first setup step is
# `copy .env.example .env`, which cannot work if it is ignored.
!.env.example
.venv/
venv/
env/
node_modules/
.next/
artifacts/
*.db
*.sqlite3
.pytest_cache/
.mypy_cache/
.coverage
htmlcov/
nul

# Machine-specific compose overrides (port remaps etc.) — never shared.
docker-compose.override.yml
.env.bak*
